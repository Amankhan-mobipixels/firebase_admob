/// This enum is responsible for defining the shimmer color in a native ad.
enum NativeShimmerColor {
  /// Displays a black shimmer effect during the ad loading state.
  black,

  /// Displays a white shimmer effect during the ad loading state.
  white,

  /// Displays a gray shimmer effect during the ad loading state.
  gray
}
